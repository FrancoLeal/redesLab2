specgram(x=datos(audio),Fs=frecuencia de muestreo, window=funcion que se aplica sobre la transformada de fourier)
NFFT=1024 $Numero de datos utilizados para 
Pxx,freqs,bins,m = plt.specgram(data,NFFT,Fs=Rate)

Nyquist rate of the signal= rate /2

cutoff_hz=1000
numtaps=1001 #Longitud del filtro (numero de coeficientes = orden del filtro +1 )

fircoef1= firwin(numtaps,cutoff/nyq_rate)


lowpassFIR
fircoef2= firwin



filtered_X = lfilter(kl